#include "main.h"

/**
 * _isdigit -this function will  checks for a digit (0 through 9)
 * int c: int to be checked
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
